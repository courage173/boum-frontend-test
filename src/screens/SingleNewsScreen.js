import React from "react";
import SingleNews from "../components/newsList/SingleNews";
const SingleNewsScreen = ({ navigation }) => {
  return <SingleNews navigation={navigation} />;
};

export default SingleNewsScreen;
